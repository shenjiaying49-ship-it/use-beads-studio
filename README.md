# use-beads-studio

一个浏览器内运行的拼豆图纸生成器。

## 功能

- 上传图片并生成编号网格
- 白色背景不计入拼豆颗数
- 上传后先按基准最小尺寸等比缩小
- 在基准尺寸上继续缩小，减少颗数和细节
- 导出当前页图纸 PNG

## 本地打开

在项目目录运行：

```bash
python3 -m http.server 8000
```

然后访问：

`http://127.0.0.1:8000/`
